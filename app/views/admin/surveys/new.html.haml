- content_for :page_title, 'New Survey'

%h1 New Survey

- form_for @survey, :url => admin_surveys_path, :html => {:multipart => true} do |f|
  
  = render :partial => 'fields', :locals => {:f => f}
  
  %div
    = f.submit 'Create Survey'