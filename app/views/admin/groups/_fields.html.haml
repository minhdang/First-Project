= f.error_messages

= required_field_legend

.required
  = f.label :title
  = f.text_field :title

%div
  = f.label :description
  = f.text_area :description

%div
  = f.label :website
  = f.text_field :website
  
%div
  = f.label :region
  = f.select :region, App.states[:US].sort.to_a.map {|a| a.reverse}, :include_blank => 'Non-regional Group'

%div
  = f.label :private, 'Private group?'
  = f.check_box :private
  %span If this is a private group you will need to approve each member who attempts to join your group.