<%=h @submission.user.first_name %>,

Tình trạng đệ trình của bạn "<%=h @submission.idea.title %>" to <%=h @submission.live_product_search.key %> gần đây đã được cập nhật.

Vui lòng truy cập <%= "#{site}/dashboard/ideas" %> để xem lại tình trạng đệ trình của bạn.

<%= signature %>